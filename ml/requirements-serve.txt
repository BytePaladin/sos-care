# Runtime dependencies for the deployed ML service only.
#
# Deliberately smaller than requirements.txt: pandas and pytest are needed to
# build the dataset and run the tests, but not to serve a prediction. Leaving
# them out keeps the deploy image ~70 MB lighter and the cold start shorter,
# which matters on a free tier that sleeps when idle.
#
# For local development (training, dataset building, tests) use requirements.txt.
scikit-learn>=1.3
numpy>=1.24
scipy>=1.10
joblib>=1.3
flask>=3.0
flask-cors>=4.0
gunicorn>=21.2
